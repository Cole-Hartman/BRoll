# Database connection (use Immich's PostgreSQL instance)
DATABASE_URL=postgresql://broll:Chickennuggets123#*@172.16.3.1:5432/broll

# Whisper model: tiny, base, small, medium, large-v2, large-v3
WHISPER_MODEL=base

# Compute type for CPU inference
WHISPER_COMPUTE_TYPE=int8

# Allowed CORS origins (comma-separated)
CORS_ORIGINS=http://truenas.angelfish-lionfish.ts.net:8080

# Public app origin used to resolve relative videoUrl values (e.g. /immich-api/...)
APP_BASE_URL=http://ix-immich-server-1:30041
